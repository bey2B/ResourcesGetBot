# Cua超级助手 — 功能更新需求

> 目标：在现有项目基础上新增/完善以下功能。优先使用代码块中的文案（注意替换变量）

---

## 1. 一个资源支持多个文件（合集资源）

### 需求说明
- 一个资源可包含多个文件（图片 / 视频 / 文档等）。
- 管理员通过 Bot 命令收集多个 `file_id`，最终打包成一个资源。

### 管理员操作流程
1. 管理员发送 `/admin`，进入管理菜单。
2. 点击「资源存储」按钮，Bot 进入「持续收集文件」状态。
3. Bot 发送以下提示：

```
请发送你要存储的内容，可以多张图片/视频/文件，支持转发
```

4. 管理员持续发送或转发文件，Bot 实时收集所有 `file_id`。
5. 收集过程中 Bot 回复：

```
✅ 文件已全部接收
📁 共添加 3 个文件
请继续发送文件，或选择操作：
```

附带三个按钮：
- 完成存储
- 取消存储
- 添加标题

6. 点击「完成存储」后，系统生成短码与唯一序号，将所有文件关联到同一资源。
7. 点击「取消存储」则丢弃本次收集的所有文件。
8. 「添加标题」允许管理员为该合集资源设置标题。

### 用户获取行为
- 用户通过短码或「我已购买的资源」获取时，Bot 发送该资源下的**全部文件**。
- 发送方式由管理后台配置：
  - 每个媒体文件单独发送
  - 或多个媒体文件组成一个消息块发送（Album）

---

## 2. 广告配置改为「独立消息」方式

### 需求说明
- 广告内容支持：文字 + 媒体 + 按钮。
- 媒体与文字内容必须来自管理员账号的转发消息。
- 按钮在管理后台额外配置。

### 配置流程
1. 管理后台进入广告配置时，Bot 向管理员发送提示（原文必须使用）：

```
您正在配置广告内容，请发送或者转发一个待以编辑完成排版好格式的文字的媒体文件给我

```

2. 管理员从自己的账号（显示 Username 与 user_id）转发一条已排版好的文字+媒体消息。
3. Bot 接收后保存该消息的完整内容（文字 + 媒体 file_id）。
4. 管理员可在后台继续为该广告配置自定义按钮。
5. 广告在资源获取前/后以**独立消息**形式发送（不再嵌入资源消息内部）。

---

## 3. 「我已购买的资源」有序列表展示

### 命令
- 用户发送 `/purchases` 触发。

### 展示规则（必须严格遵守）
功能描述：资源有序列表（分页）

在 Telegram 中以一条消息展示资源文件的有序列表。

核心行为：
- 每页固定显示 10 条资源，按序号排列（1、2、3…）
- 消息标题显示当前页码，例如「我已购买的资源·第 2/3 页」
- 每条资源以「序号. 标题」形式展示，标题前可带 emoji 或标签
- 消息底部有「上一页」「下一页」按钮，用于前后翻页
- 消息块下方带有序号的按钮，用户点击任意一条资源对应的按钮后，机器人立即发送该资源的内容
- 翻页后更新同一条消息的内容与按钮

交互要点：
- 点击具体资源条目按钮 → 直接投递该资源内容给用户

每页严格 10 条，不足 10 条时只显示实际数量

---

## 4. 广播增加二次确认

### 需求说明
- 管理员通过 `/admin` 进入广播入口。
- 发送广播前必须经过二次确认流程。
- 广播完成后返回详细进度报告（成功 / 失败 / 跳过 / 总计）。

### 流程文案（xxx替换为变量）
```
您正在向 xxx 位用户发送广播消息

请向我发送或转发一条消息，消息内容可以是任何形式——文字、照片、视频，甚至是贴纸。您还可以转发频道中的消息，以便查看其实际浏览量。

输入 /cancel 取消操作。
```

- 管理员发送/转发消息后，Bot 再次确认「是否立即发送」或进入定时设置。
- 确认后开始广播，并返回进度统计。

---

## 5. 合集资源的发送方式（与第1点配套）

当用户通过短码或「我已购买的资源」获取资源时：
- Bot 发送该资源包含的**全部文件**。
- 支持两种模式（管理后台可配置）：
  1. 每个媒体文件单独发送
  2. 多个媒体文件组成一个消息块（Telegram Album）发送


1. 一个资源支持多个文件（管理员持续收集 file_id → 合集资源）
2. 广告配置改为独立消息方式（文字+媒体+按钮，来自管理员转发）
3. `/purchases` 命令以分页有序列表展示已购买资源（每页10条，可点击获取）
4. 广播增加二次确认流程
5. 合集资源支持「单独发送」或「消息块（Album）」两种发送模式（后台可配置）

## 6. 修复用户封禁/解封接口类型错误

### 问题
- 前端发送 `isBanned: 0 | 1`（number）
- 后端校验要求 `isBanned` 必须是 `boolean`
- 导致接口返回 400，封禁/解封实际未执行
- 前端错误处理只解析 `error.message`，后端返回的是字符串 `error`，真实错误被吞掉，只显示「请求失败（HTTP 400）」

### 修复要求（最小化）
1. 前端发送时把 `0 | 1` 转成真正的布尔值 `false | true`
2. 前端错误处理兼容后端返回的字符串形式 `error`
3. 不改变后端校验逻辑，不扩大修改范围
