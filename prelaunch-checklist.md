# Cua超级助手 上线前测试 Checklist

> 依据：`requirements.md` + `requirements-update.md` + `README.md`
> 用法：逐项执行并勾选；建议在「测试结果」列记录测试环境、操作步骤、实际结果与备注。

## 一、功能测试项

### 1. 部署与环境

- [ ] D1 数据库已创建并绑定真实 `database_id`（非占位 UUID）
- [ ] `wrangler.toml` / `wrangler.admin.toml` 配置正确
- [ ] 环境变量已配置：`BOT_TOKEN`、`ADMIN_IDS`、`ADMIN_PASSWORD_HASH`、`JWT_SECRET`、`WEBHOOK_SECRET`、`ADMIN_IP_WHITELIST`、`ADMIN_CORS_ORIGIN`
- [ ] `.dev.vars` 未提交到仓库，生产使用 `wrangler secret put`
- [ ] `npm run build` 通过（typecheck + admin 构建 + wrangler dry-run）
- [ ] `npm test` 全部通过
- [ ] `npm run db:local` / 远程迁移成功
- [ ] `GET /health` 返回 200
- [ ] Telegram Webhook 设置成功（含 `secret_token`）
- [ ] Cron Triggers 已生效（每日维护、广播检查、统计预聚合）

### 2. Bot 基础

- [ ] `/start` 欢迎语正常
- [ ] Deep Link `start={短码}` 可触发资源流程
- [ ] Deep Link `start=inv_{user_id}` 可记录新用户邀请
- [ ] 强制关注：开关生效、支持多频道、未关注给出清晰引导、已关注可放行
- [ ] 群组内发送短码自动回复
- [ ] 自动删除秒数配置生效（用户消息与机器人回复）
- [ ] 非管理员使用 `/admin` 被拒绝
- [ ] 普通用户无法使用管理员命令与回调按钮

### 3. 资源系统

- [ ] 管理员转发媒体后自动提取 `file_id`
- [ ] 生成 8 位唯一短码（碰撞时重试）
- [ ] 唯一序号从 0 递增
- [ ] 单文件资源创建成功
- [ ] 合集资源：持续收集多个文件且计数正确
- [ ] 合集资源：「完成存储」生成短码并关联全部文件
- [ ] 合集资源：「取消存储」丢弃本次文件
- [ ] 合集资源：「添加标题」保存标题
- [ ] 免费资源直接交付
- [ ] 付费资源按积分价格购买并扣款
- [ ] 积分不足时提示签到或邀请
- [ ] 同一用户短时间重复请求同一资源被限流
- [ ] 「一个文件一个消息块」开关生效
- [ ] 每次获取写入下载记录（次数与时间）
- [ ] 资源列表与详情在后台可查询

### 4. 合集资源发送模式（更新需求）

- [ ] 后台 `resource_send_mode=single`：每个文件单独发送
- [ ] 后台 `resource_send_mode=album`：photo/video 组成 Album 发送（每块最多 10 条）
- [ ] album 模式：document/audio 等不支持 Album 的类型单独发送
- [ ] album 模式：仅 1 个媒体时不分组
- [ ] 用户通过短码获取合集资源时收到全部文件
- [ ] 用户通过「我已购买的资源」获取合集资源时收到全部文件

### 5. 用户与积分

- [ ] 私聊/群组发送「签到」每日 +1
- [ ] 同日重复签到不重复加分
- [ ] 邀请新用户成功 +5（仅新用户且一次性）
- [ ] 积分余额查询正确
- [ ] 积分购买付费资源并生成购买记录
- [ ] 积分流水与购买记录正确
- [ ] 封禁用户不能继续获取资源/积分（按产品定义验证）

### 6. /purchases 分页列表（更新需求）

- [ ] `/purchases` 命令可正常触发
- [ ] 每页严格显示 10 条，不足 10 条显示实际数量
- [ ] 标题格式为「我已购买的资源·第 X/Y 页」
- [ ] 条目格式为「序号. 标题」
- [ ] 「上一页」「下一页」按钮可翻页并编辑同一条消息
- [ ] 点击序号按钮立即发送该资源全部内容
- [ ] 仅显示已购买付费资源，免费资源不出现
- [ ] 其他用户无法操作本人的分页/资源按钮

### 7. 广告（更新需求）

- [ ] 广告配置入口文案与需求完全一致
- [ ] 管理员转发的文字+媒体消息被保存（文字 + `file_id`）
- [ ] 非管理员转发的消息不能进入广告配置
- [ ] 后台可配置广告按钮（text + url，可增删）
- [ ] 上方/下方广告以独立消息发送
- [ ] 资源获取前/后广告顺序正确
- [ ] 广告按权重轮换，禁用广告不展示
- [ ] 无可用广告时不发送空广告

### 8. 广播（更新需求）

- [ ] `/admin` 广播入口显示当前用户总数
- [ ] 二次确认文案与需求完全一致
- [ ] 发送/转发消息后出现「立即发送 / 定时发送 / 取消」确认
- [ ] 确认后立即广播成功
- [ ] 定时广播：一次性、每天、每周、每月均生效
- [ ] `/cancel` 可中途取消
- [ ] 进度报告包含成功 / 失败 / 跳过 / 总计
- [ ] 广播历史记录可查询

### 9. Vue 管理后台

- [ ] 登录页正常，密码错误有提示
- [ ] 连续输错 10 次后锁定一段时间
- [ ] 仪表盘：总用户、今日下载、资源数、广播数
- [ ] 仪表盘：近 7 天 / 30 天趋势
- [ ] 仪表盘：24 小时下载热力图与积分循环数据
- [ ] 资源管理：列表、搜索、排序、分页
- [ ] 资源管理：新增、编辑、删除、付费设置、合集文件数
- [ ] 用户管理：积分调整、封禁 / 解封（`isBanned` 布尔修复生效）
- [ ] 用户管理：积分日志、下载记录、操作记录
- [ ] 系统设置：强制关注、频道绑定、群组回复、自动删除、风控、发送模式
- [ ] 系统设置：广告按钮配置保存与回显
- [ ] 日志页：操作日志完整可查
- [ ] 前端错误提示兼容后端字符串 `error`
- [ ] 桌面端与移动端布局正常，无文字遮挡

### 10. Cron 与统计

- [ ] 每日 UTC 00:00 清理过期签到与限流记录
- [ ] 每 15 分钟检查并执行到期定时广播
- [ ] 每日 UTC 00:30 预聚合统计快照
- [ ] 所有时间使用 UTC 存储，展示时本地化

## 二、安全测试项

### 1. 认证与授权

- [ ] 管理后台 API 除 login 外全部要求 JWT
- [ ] 无 token / 错误 token / 过期 token 均返回 401
- [ ] `JWT_SECRET` 未配置时登录明确失败
- [ ] 后台密码使用 bcrypt 校验
- [ ] 连续 10 次登录失败触发锁定
- [ ] 非 `ADMIN_IDS` 配置的 user_id 不能执行管理员命令
- [ ] 启用 IP 白名单后，非白名单 IP 被拒绝
- [ ] 生产环境 CORS 配置为具体域名，不使用 `*`
- [ ] Webhook 缺少/错误 secret 时返回 401
- [ ] 前端收到 401 自动跳转登录，不缓存敏感数据

### 2. 输入与注入

- [ ] 所有数据库查询使用参数绑定，无 SQL 注入
- [ ] API 输入校验：`fileId`、`price`、`points`、`isBanned`、settings 值
- [ ] 短码长度与字符集校验
- [ ] 资源 ID、用户 ID 仅接受正整数
- [ ] 管理后台标题/标签/广告文案无 XSS（安全转义或净化）
- [ ] 普通用户不能越权修改他人积分或封禁状态
- [ ] settings key 有白名单或类型校验

### 3. 数据与密钥

- [ ] 仓库内无真实密钥（`.dev.vars`、wrangler secret 未提交）
- [ ] `ADMIN_PASSWORD_HASH` 为强密码的 bcrypt 哈希
- [ ] `JWT_SECRET` 为强随机值且未使用占位符
- [ ] `BOT_TOKEN` 未出现在前端代码、日志或响应中
- [ ] 日志不包含密码、token、完整密钥
- [ ] `admin_logs` 完整记录关键操作（登录、资源、广播、设置）

### 4. 滥用与风控

- [ ] 资源请求限流真实生效
- [ ] 签到、购买、邀请幂等，不能重复加分或重复扣款
- [ ] 广播仅管理员可触发且必须二次确认
- [ ] 超大 Webhook 载荷 / 异常 JSON 不导致 Worker 崩溃
- [ ] Cron 各任务独立异常处理，互不影响
- [ ] 群组与私聊命令无越权滥用

### 5. 部署与依赖

- [ ] Worker 仅绑定必要资源（最小权限）
- [ ] 生产 `database_id` 已替换占位 UUID
- [ ] 管理后台通过 HTTPS 访问
- [ ] setWebhook 的 URL 为 HTTPS
- [ ] `npm audit` 无高危漏洞
- [ ] 依赖版本可复现（lockfile 一致）
- [ ] 远程 D1 migrations 已成功应用

## 测试记录

| 编号 | 模块 | 测试结果 | 执行人 | 日期 | 备注 |
|---|---|---|---|---|---|
|  |  |  |  |  |  |
|  |  |  |  |  |  |
