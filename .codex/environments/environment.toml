# THIS IS AUTOGENERATED. DO NOT EDIT MANUALLY
version = 1
name = "ResourcesGetBot"

[setup]
script = '''
[setup]
script = """
# 激活虚拟环境
.venv\Scripts\Activate.ps1

# 安装依赖
pip install -r requirements.txt

# 创建数据目录
if (-not (Test-Path data)) { New-Item -ItemType Directory -Path data }

# 显示环境信息
python --version
"""
'''

[[actions]]
name = "运行程序"
icon = "run"
command = '''
[run]
script = """
# 先杀旧进程
Get-Process -Name python -ErrorAction SilentlyContinue | Stop-Process -Force
Start-Sleep -Seconds 1

# 启动 Bot（后台）
Start-Process -WindowStyle Hidden -FilePath ".venv\Scripts\python.exe" -ArgumentList "main.py"

# 启动 Web 后台（后台）
Start-Process -WindowStyle Hidden -FilePath ".venv\Scripts\python.exe" -ArgumentList "-m", "uvicorn", "web:app", "--host", "0.0.0.0", "--port", "8000"

Start-Sleep -Seconds 2
Write-Host "Bot + Web 已启动"
"""
'''

[[actions]]
name = "检查 bot 是否在跑"
icon = "tool"
command = "Get-Process -Name python -ErrorAction SilentlyContinue | Format-Table Id, ProcessName"
